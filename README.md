# Servers Crash Downdetector
A means of verifying a connection to a server via a TCP connection over a domain/ip.

![Python](https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white)![Flask](https://img.shields.io/badge/Flask-000000?style=for-the-badge&logo=flask&logoColor=white)![Telegram](https://img.shields.io/badge/Telegram-26A5E4?style=for-the-badge&logo=telegram&logoColor=white)![SQLite](https://img.shields.io/badge/SQLite-003B57?style=for-the-badge&logo=sqlite&logoColor=white)

#### Обозначения версий:
* T — Протестировано.
* S — Стабильно.
* NT — Не тестировалось.
* NS — Не стабильно.
* HE — Имеются ошибки.

Комплексная система для мониторинга доступности серверов. Включает в себя Telegram-бота для отправки уведомлений и администрирования, а также публичный веб-интерфейс для отображения статусов серверов в реальном времени.

## 🚀 Основные возможности

*   **Telegram-бот для администрирования**:
    *   Добавление/удаление серверов для мониторинга.
    *   Управление списком администраторов (чатов для оповещений).
    *   Настройка интервала проверок.
    *   Публикация/скрытие серверов на общедоступной веб-странице.
    *   Мгновенная проверка статуса всех серверов по команде.
    *   Уведомления о падении и восстановлении серверов.
*   **Веб-интерфейс**:
    *   **Публичная страница**: Отображает статус (`Online`/`Offline`) и пинг только для "публичных" серверов. Данные обновляются динамически без перезагрузки страницы.
    *   **Статистика падений**: Таблица с расчетом аптайма за последние 24 часа и указанием периодов недоступности.
    *   **Секретная админ-панель**: Отдельная страница со статусом всех серверов (включая скрытые), подробной информацией о времени последней проверки и конфигурацией системы.
*   **Система мониторинга**:
    *   Асинхронные проверки для высокой производительности.
    *   Настраиваемые параметры проверок (таймаут, количество попыток, задержка).
    *   Ведение истории падений и восстановлений в базе данных SQLite.

## ⚙️ Технологический стек

*   **Бэкенд**: Python
*   **Веб-фреймворк**: Flask
*   **Telegram-бот**: `python-telegram-bot`
*   **База данных**: SQLite(.db)
*   **Веб-сервер (рекомендуемый)**: Gunicorn с `gevent`
*   **Фронтенд**: HTML, CSS, ванильный JavaScript (для динамических обновлений и логики доступа к админ-панели)

## 🔧 Установка и запуск

#### 1. Клонирование репозитория

```bash
git clone https://github.com/SL1ZN1T3L/Servers-Crash-Downdetector.git
cd Servers-Crash-Downdetector
```

#### 2. Создание виртуального окружения и установка зависимостей

Рекомендуется использовать виртуальное окружение для изоляции зависимостей.

```bash
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
```

Создайте файл `requirements.txt` со следующим содержимым:

```txt
flask
python-telegram-bot[job-queue]
python-dotenv
gunicorn
gevent
pytz
Flask-WTF
Flask-Talisman
```

И установите зависимости:
```bash
pip install -r requirements.txt
```

#### 3. Конфигурация

Создайте файл `.env` в корне проекта и заполните его по примеру ниже:

```env
# Обязательный токен вашего Telegram-бота, полученный от @BotFather
TELEGRAM_BOT_TOKEN="123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"

# Секретный ключ для Flask. Используйте длинную случайную строку
FLASK_SECRET_KEY="your-super-secret-and-long-random-string-for-flask"

# (Опционально) Секретный путь для админ-панели на сайте. Если не указан, используется значение по умолчанию.
ADMIN_PANEL_SECRET_PATH="my-secret-admin-panel"

# (Опционально) Ваш тег в Telegram для контактов на публичной странице.
TELEGRAM_TAG="@your_telegram_username"

# --- Параметры проверки серверов (можно оставить по умолчанию) ---
# Количество попыток подключения к серверу перед признанием его "упавшим"
CHECK_RETRIES=3
# Задержка между попытками в секундах
CHECK_RETRY_DELAY=2
# Таймаут для одной попытки подключения в секундах
CHECK_SINGLE_TIMEOUT=5
# Количество последовательных неудачных циклов проверок, после которых отправляется уведомление
FAILURE_THRESHOLD=3
```

#### 4. Настройка администраторов и серверов

*   **Администраторы**: При первом запуске бота ни одного администратора не будет. Первый, кто напишет боту, получит инструкцию как добавить себя командой `/add_chat <ID>`. Свой ID вы узнаете в этом сообщении.
*   **Серверы**: При первом запуске база данных будет пустой. Бот автоматически импортирует серверы из файла `servers.json`, если таковой присутствует. Убедитесь, что он содержит нужные вам серверы.

    `servers.json`:
    ```json
    [
      {
        "name": "Moscow",
        "host": "msk.example.com",
        "port": 443
      },
      {
        "name": "Germany",
        "host": "gr.example.com",
        "port": 443
      }
    ]
    ```

#### 5. Запуск проекта

Проект состоит из двух частей: бота и веб-сервера. Их нужно запускать в разных терминалах. Веб-сервер не проверяет соединение, только вывод информацию.

**Запуск Telegram-бота:**

```bash
python monitoring_bot.py
```
При первом запуске он создаст и мигрирует базу данных `monitoring.db`.

**Запуск веб-приложения (Gunicorn - для продакшена):**

Это рекомендуемый способ запуска, указанный вами.

```bash
gunicorn --graceful-timeout 3 --worker-class gevent --workers 3 --bind 0.0.0.0:12918 webapp:app
```
*Замените `0.0.0.0` на конкретный IP, если это необходимо.*

## ▶️ Использование

### Команды Telegram-бота

Все команды администрирования доступны через инлайн-кнопки в меню бота (`/start`) или через текстовые команды.

| Команда         | Аргументы                        | Описание                                                                  |
| --------------- | -------------------------------- | ------------------------------------------------------------------------- |
| `/start`        | -                                | Открывает главное меню с кнопками.                                        |
| `/status`       | -                                | Показывает текущие настройки и список всех отслеживаемых серверов.        |
| `/check_now`    | -                                | Запускает немедленную проверку всех серверов и присылает отчет.           |
| `/add_server`   | `<имя> <хост> <порт>`            | Добавляет новый сервер для мониторинга. Имя может состоять из нескольких слов. |
| `/remove_server`| `<имя>`                          | Удаляет сервер из мониторинга.                                            |
| `/publish`      | `<имя>`                          | Делает сервер видимым на публичной веб-странице.                          |
| `/hide`         | `<имя>`                          | Скрывает сервер с публичной веб-страницы (мониторинг продолжается).       |
| `/add_chat`     | `<id чата/пользователя>`         | Добавляет ID в список для получения уведомлений (делает администратором).  |
| `/remove_chat`  | `<id чата/пользователя>`         | Удаляет ID из списка уведомлений.                                         |
| `/set_interval` | `<секунды>`                      | Изменяет интервал проверки всех серверов (рекомендуется >= 30).           |
| `/restart`      | -                                | Перезапускает бота (может требовать настройки на уровне системы/docker).  |
| `/help`         | -                                | Показывает справку по командам.                                           |
| `/cancel`       | -                                | Отменяет текущую операцию (например, добавление сервера).                 |

### Доступ к Админ-панели

Доступ к веб-панели администратора скрыт за нетривиальной последовательностью действий на главной странице (логика в `secret_logic.js`). Это "секретное рукопожатие" для предотвращения случайного доступа.

**Чтобы получить доступ:**

1.  Откройте главную страницу сайта.
2.  Кликните в **левый нижний** угол экрана (появится невидимая зона).
3.  Подождите **2-3.5 секунды** и кликните в **правый верхний** угол.
4.  Подождите **3-4.5 секунды** и кликните в **левый верхний** угол.
5.  Подождите **2-3 секунды** и кликните **снова в левый верхний** угол.
6.  Если все тайминги соблюдены, в левом верхнем углу появится кликер с надписью "START".
7.  **У вас есть 3 секунды, чтобы сделать 180 кликов** по этому кликеру.
8.  При успехе вас автоматически перенаправит на секретный URL админ-панели (`/your-secret-path`). В случае неудачи страница перезагрузится.

## 📁 Структура проекта

```
.
├── webapp.py               # Flask веб-приложение (публичная часть и админка)
├── monitoring_bot.py       # Основной скрипт Telegram-бота
├── database.py             # Модуль для работы с базой данных SQLite
├── config.json             # Конфигурация ID чатов и интервала проверки
├── servers.json            # Начальный список серверов для импорта
├── .env                    # Файл с секретными переменными окружения (нужно создать)
├── requirements.txt        # Список зависимостей Python (нужно создать)
├── templates/              # HTML шаблоны Flask
│   ├── index.html          # Главная публичная страница
│   ├── admin.html          # Скрытая страница админ-панели
│   ├── layout.html         # (если есть) базовый шаблон
│   └── partials/           # Фрагменты HTML для динамической загрузки
│       ├── server_list.html
│       └── downtime_table.html
└── static/                 # Статические файлы (CSS, JS, изображения)
    ├── style.css
    ├── dynamic_update.js   # JS для обновления данных на главной странице
    └── secret_logic.js     # JS для "секретного рукопожатия"
```

## 📄 Лицензия

Проект распространяется под лицензией GPLv3. Смотрите файл `LICENSE` для подробностей.
